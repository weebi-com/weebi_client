name: users_weebi
description: "User management package for Weebi with protobuf mirroring capabilities, authentication, and comprehensive UI components including beautiful user creation flow"
version: 1.1.3
homepage: https://weebi.com
repository: https://github.com/weebi-app/weebi-client/packages/users
issue_tracker: https://github.com/weebi-app/weebi-client/issues

environment:
  sdk: ">=3.12.2 <4.0.0"
  flutter: ">=3.44.8"

dependencies:
  flutter:
    sdk: flutter
  ####
  # weebi kitchen below
  #### 
  protos_weebi: ^1.4.0
  auth_weebi: ^1.1.0
  entitlements_weebi: ^1.0.1
  fl_country_code_picker_weebi: ^0.2.3

  provider: ^6.1.4
  flutter_secure_storage: ^10.3.0 #
  # Utilities
  #collection: ^1.19.1 read from auth
  intl: any #^0.20.2
  logging: ^1.2.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  lints: ^5.0.0
